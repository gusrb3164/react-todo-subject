import React from "react";
import { Todo } from "../App";

type TodoAdderProps = {
  todoList: Todo[];
};

const TodoAdder = ({ todoList }: TodoAdderProps) => {
  return <div>TodoAdder</div>;
};

export default TodoAdder;
